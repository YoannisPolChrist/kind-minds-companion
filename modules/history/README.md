# history

Aggregates completed exercises and check-ins into a single client history feed.

## Public API

- `fetchHistoryFeed(userId, options)`
- `sortHistoryEntries(entries)`
- `groupHistoryByWeek(entries)`
- `flattenHistoryGroups(groups)`
- `formatHistoryDate(date, locale)`
- `formatHistoryWeekLabel(date, locale)`
