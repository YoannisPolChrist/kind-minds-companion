# Scheduling Module

Handles appointment sync, calendar provider selection and calendar link generation.

## Public API

- `getCalendarTargets()`
- `syncAppointmentToDevice(details, options)`
- `buildCalendarLinks(details)`
