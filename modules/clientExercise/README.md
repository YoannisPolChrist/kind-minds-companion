# clientExercise

Client-facing exercise loading, completion and PDF export helpers.

## Public API

- `fetchClientExercise(exerciseId, options)`
- `completeClientExercise(exerciseId, payload)`
- `sanitizeExerciseAnswers(answers)`
- `buildExercisePdfHtml(exercise, answers)`
